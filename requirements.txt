numpy>=1.24
torch>=2.2
# Optional: only needed for the real-model probe scripts (ssa_swap, real_keys,
# longctx_keys, longctx_probe, gemma_keys), which download pretrained models.
# The gemma_* scripts need transformers>=5.0 (the gemma4 architecture); everything
# else is fine on >=4.40.
transformers>=4.40
datasets>=2.18
# Optional: the GPU 12M-scale scripts and the CCC selector (ivf_kernel, ivf_decode,
# faiss_router, router_gpu_compare, cascade_router) need faiss on the GPU
# (Linux x86_64 wheels; tests skip cleanly without it):
#   pip install faiss-gpu-cu12
# Optional: test runner
pytest>=7.0
